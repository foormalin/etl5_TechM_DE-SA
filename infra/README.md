# Infrastructure

Local platform configuration for Compose, Keycloak and observability. The Helm
directory records the production deployment boundary that requires a selected
cloud and secret manager.
