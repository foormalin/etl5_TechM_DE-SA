# UI kit

Monochrome design tokens and reusable React primitives currently live in
`src/styles.css` and `src/main.jsx`. They remain intentionally code-native so the
public demo has no asset pipeline dependency.
