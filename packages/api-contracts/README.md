# API contracts

`contracts/openapi/techm-public-v1.json` is the repository baseline. Domain
services additionally expose generated `/openapi.json`; CI must reject breaking
changes or an uncommitted generated diff.
